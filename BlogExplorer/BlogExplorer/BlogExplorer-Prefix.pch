//
// Prefix header for all source files of the 'WebView' target in the 'WebView' project
//

#ifdef __OBJC__
    #import <Cocoa/Cocoa.h>
#endif

#ifdef DEBUG
    #define NSLog(fmt, ...) NSLog((@"[%s][%d]" fmt), __FUNCTION__, __LINE__, ##__VA_ARGS__);
#else
    #define NSLog(...);
#endif
